{
  "name": "@kubohiroya/turbowarp-runtime-expression",
  "version": "0.5.0",
  "description": "A safe condition evaluator and conditional broadcast monitor for TurboWarp runtime variables.",
  "author": "Hiroya Kubo <hiroya@cuc.ac.jp>",
  "type": "module",
  "license": "MPL-2.0",
  "homepage": "https://kubohiroya.github.io/turbowarp-runtime-expression/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kubohiroya/turbowarp-runtime-expression.git"
  },
  "bugs": {
    "url": "https://github.com/kubohiroya/turbowarp-runtime-expression/issues"
  },
  "exports": {
    "./composition": {
      "types": "./dist/types/composition.d.ts",
      "import": "./dist/composition.js"
    },
    "./runtime-expression.js": "./dist/runtime-expression.js",
    "./dist/runtime-expression.js": "./dist/runtime-expression.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "unpkg": "./dist/runtime-expression.js",
  "jsdelivr": "./dist/runtime-expression.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "build": "pnpm run build:extension && pnpm run build:composition && pnpm run build:types",
    "build:extension": "vite build",
    "build:composition": "vite build --config vite.composition.config.ts",
    "build:types": "tsc --project tsconfig.build.json",
    "types:composition:check": "tsc --project tsconfig.composition-consumer.json",
    "test": "vitest run",
    "docs": "node scripts/generate-readme.mjs",
    "docs:check": "node scripts/generate-readme.mjs --check",
    "check:dist": "pnpm run build && git diff --exit-code -- dist",
    "repo:check": "node scripts/check-repo.mjs",
    "pack:check": "npm pack --dry-run --ignore-scripts",
    "release:check": "npm publish --dry-run --access public",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build && pnpm run types:composition:check && pnpm run docs:check && pnpm run check:dist && pnpm run repo:check && pnpm run pack:check",
    "prepack": "pnpm run build"
  },
  "devDependencies": {
    "@kubohiroya/vite-plugin-turbowarp-extension": "0.1.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  },
  "keywords": [
    "turbowarp",
    "scratch",
    "extension",
    "expression",
    "runtime-variables"
  ]
}
